package com.voyago.config;

public enum DatabaseOperation {
    SELECT,  // GET    → api_select
    INSERT,  // POST   → api_insert
    UPDATE,  // PUT    → api_update
    DELETE   // DELETE → api_delete
}
